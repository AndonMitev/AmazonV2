const state = {
    userData: {
        email: '',
        id: ''
    }
};

const actions = {};

const mutations = {};

const getters = {
    userData: state => state.userData
};

export default {
    state,
    actions,
    mutations,
    getters
}
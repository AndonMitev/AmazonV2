import categoryServices from '../services/categories';

const state = {
    categories: []
};

const actions = {
    async setCategories({ commit }, payload) {

    }
}

const mutations = {};

const getters = {};

export default {
    state,
    actions,
    mutations,
    getters
}